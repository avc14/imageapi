name: conditional checks
on: workflow_dispatch
env:
  username: ${{ secrets.username }}
jobs: 
  job1:
    runs-on: ubuntu-latest
    container:
      image: python
    steps:
    - name: failing-test
      continue-on-error: true
      run: kouuoijn
    - name: i will not run
      run: echo ${{ github }}

  job2:
    runs-on: ubuntu-latest
    needs: job1
    environment: developement
    if: always()
    steps:
    - name: clear
      run: echo "clearing the workspace for username ${{env.username}} $NAME"
      
    
